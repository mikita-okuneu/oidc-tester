name: Cycode Scan (OIDC)

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  id-token: write
  contents: read

jobs:
  cycode:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python for Cycode CLI
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Cycode CLI
        run: |
          pip3 install --upgrade cycode

      - name: Get OIDC token
        id: oidc
        run: |
          TOKEN=$(curl -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
                      "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=cycode")
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Set environment variable
        run: echo "CYCODE_API_URL=${{ secrets.CYCODE_API_URL }}" >> $GITHUB_ENV

      - name: Run scans
        run: |
          cycode scan secrets .
          cycode scan sast .
          cycode scan iac .